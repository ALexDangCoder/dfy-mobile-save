import 'package:flutter/material.dart';
class PropertiesWithAddBtn extends StatelessWidget {
  const PropertiesWithAddBtn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
